// show layer
window.addEventListener('DOMContentLoaded', function () {
    // $('.layer-trigger').each(function () {
    //     alert('tigers');
    // })
    // // $('.layer-trigger').hover(function () {
    // //     let id = $(this).data('id');
    // //     $('#layer' + id).removeClass('hidden');
    // // }, function () {
    // //     $('#layer' + id).addClass('hidden');
    // // })
    console.log($('.layer-trigger').length);
});
